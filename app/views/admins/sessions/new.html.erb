<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title></title>
    <meta name="description" content="sistema control salitas">
    <meta name="author" content="programador juan rodriguez">
    <!-- Le styles -->
    <%= stylesheet_link_tag "bootstrap.min" %>
    <style type="text/css">
    body {
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
    }

    .form-signin {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
        -moz-border-radius: 5px;
        border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
        -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
        box-shadow: 0 1px 2px rgba(0,0,0,.05);
    }
    .form-signin .form-signin-heading,
    .form-signin .checkbox {
        margin-bottom: 10px;
    }
    .form-signin input[type="text"],
    .form-signin input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
    }

    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="span4 offset4 well">
                <legend>Please Sign In</legend>
                <%=render :partial=>"/partials/notices" %>
                <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
                <%= f.email_field :email, :autofocus => true,class: "span4",placeholder: "Email" %>
                <%= f.password_field :password,class: "span4",placeholder: "Password" %>
                <% if devise_mapping.rememberable? -%>
                <label class="checkbox">
                    <div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
                </label>
                <% end -%>
                <%= f.submit "Sign in",class: "btn btn-info btn-block" %>
                <% end %>
            </div>
        </div>
    </div>
</body>
</html>
<%#= render "admins/shared/links" %>
